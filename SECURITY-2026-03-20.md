# Security note — 2026-03-20

## Host hardening applied

- OpenClaw credentials directory tightened from `775` to `700`
- `ufw` enabled
  - incoming: `deny`
  - outgoing: `allow`
- `openssh-server` not installed / not active
- `avahi-daemon` disabled
- automatic security updates already active
- OpenClaw gateway remains bound to loopback

## OpenClaw config cleanup

- `channels.telegram.groupAllowFrom` set to Dominic's Telegram user ID so `groupPolicy: "allowlist"` is no longer empty
- ineffective `gateway.nodes.denyCommands` entries removed to avoid false confidence

## Notes

- `gateway.trustedProxies` warning is acceptable while the gateway stays local-only (`bind: loopback`) and is not exposed behind a reverse proxy.
- If the Control UI is ever published behind a reverse proxy or tunnel, revisit `trustedProxies`, auth scopes, and exposure rules before opening access.
- If remote administration is ever needed later, prefer VPN/Tailscale or key-only SSH rather than password-based SSH.
